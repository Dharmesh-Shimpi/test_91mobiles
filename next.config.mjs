/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"beta.32mobiles.com",
			"rukminim2.flixcart.com",
			"plus.unsplash.com",
			"example.com",
		],
	},
};

export default nextConfig;
